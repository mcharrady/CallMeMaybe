- Qwen/Qwen3-0.6B (default) -> d
- Large Language Models (LLMs) -> d
- Function calling enables LLMs to:
	• Interact with external systems: Call APIs, query databases, control devices
	• Execute code: Perform calculations, data transformations, file operations
	• Chain operations: Break complex tasks into executable steps
	• Provide structured output: Generate JSON, XML, or other machine-readable
	    formats
	• Extract structured data from unstructured text: For example, given a large
	    book, extract fields such as {protagonist name, protagonist sex, protagonist
	    age}
- constrained decoding -> og
- Function Calling
- Tokenization -> d
- embedding -> d
- Logits -> d
- autoregression(autoregressive modle)
- JSON Schema -> d
- LLM SDK
- Finite State Machine (FSM)
- Function Selection
- Argument Extraction
- Output Validation
- Prompt > Tokenizer > Input IDs > LLM > Logits > Choose next token > Repeat(generation pipeline)
- api(brief)






====================================================================================

====================================================================================

====================================================================================

====================================================================================




Function: fn_substitute_string_with_regex\n"
        "User Request: Replace 'blue' with 'red' in 'the blue sky'\n"
        "Function call: fn_substitute_string_with_regex{\"source_string\":\"the blue sky\",\"regex\":\"blue\",\"replacement\":\"red\"}



raw_params_text = "".join(
        model.decode([t]) for t in generate_ids[len(name_progress):]
    )




"Extract parameters in JSON format.\n\n"
        "Example:\n"
        "Function: fn_add_numbers\n"
        "User Request: Add 5 and 3\n"
        "Function call: fn_add_numbers{\"a\":5,\"b\":3}\n\n"
        f"Function: {chosen_def.name}\n"
        f"Description: {chosen_def.description}\n"
        f"User Request: {prompt}\n"
        "Function call: "






import numpy as np

logits = np.array(logits)              # convert list[float] -> ndarray
mask = np.full(len(logits), -np.inf)   # np.full(shape, fill_value)
mask[valid_tokens] = logits[valid_tokens]   # valid_tokens: list/array of int indices
next_token_id = int(np.argmax(mask))







- src/
  │
  ├── __init__.py
  ├── __main__.py
  ├── parser.py reads and validates the input JSON files.
  ├── models.py contains your Pydantic models.
  ├── decoder.py implements the constrained decoding logic.
  ├── generator.py performs token-by-token generation.
  ├── validator.py checks the generated JSON against the function schema.
  └── output.py writes the final results.


Parser: (wip)
    - load functions_definition.json
    - Open file > json.load() > list[dict] > Pydantic validation > list[FunctionDefinition]
    - load function_calling_tests.json
    - Open file > json.load() > list[dict] > list[Prompt]
    - Errors:
        - File doesn't exist
        - FileNotFoundError
        - Invalid JSON
        - json.JSONDecodeError
        - JSON structure is wrong
        - Pydantic ValidationError
        - permission error

generator: (wip)
    - encode: prompt ---> token_ids
    - 




You are a function-calling JSON object builder.

Given a user request, generate a JSON object with exactly these keys:

- "prompt": the original user request.
- "name": the function to call.
- "parameters": all required arguments with the correct values and types.

Rules:

- To select the function, read the descriptions of all available functions and choose the one whose description best matches the user's request. Do not choose a function based only on its name.
- After selecting the function, the parameter names and types are already known from its definition.
- Your task is only to determine the correct value for each parameter.
- Use the function description, parameter definition, and the user's request to infer every parameter value.
- Return values that match the required parameter types.

Available functions:








[
  {
    "prompt": "What is the sum of 2 and 3?"
  },
  {
    "prompt": "What is the sum of 265 and 345?"
  },
  {
    "prompt": "Greet shrek"
  },
  {
    "prompt": "Greet john"
  },
  {
    "prompt": "Reverse the string 'hello'"
  },
  {
    "prompt": "Reverse the string 'world'"
  },
  {
    "prompt": "What is the square root of 16?"
  },
  {
    "prompt": "Calculate the square root of 144"
  },
  {
    "prompt": "Replace all numbers in \"Hello 34 I'm 233 years old\" with NUMBERS"
  },
  {
    "prompt": "Replace all vowels in 'Programming is fun' with asterisks"
  },
  {
    "prompt": "Substitute the word 'cat' with 'dog' in 'The cat sat on the mat with another cat'"
  }
]




